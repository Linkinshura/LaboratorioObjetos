# Normalizacion de Datos
´´´java
dato= mouseX;

min=0;

max=width;
norm= dato/max;

colorMax=255;
colorF=norm*colorMax;
radioF=norm*radioMaximo:
´´´


## Funciones Matematicas Utiles

´´´java
resultados = min(val1,val2);

max(val1,val2);

constrain(val,min,max);

map(val,min,max,Nnin,Nmax);

lerp(val1,val2, indexinterpool);
´´´
